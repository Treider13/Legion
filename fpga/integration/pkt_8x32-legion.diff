diff --git a/hdl/fpga/platforms/common/bladerf/software/bladeRF_nios/src/pkt_8x32.c b/hdl/fpga/platforms/common/bladerf/software/bladeRF_nios/src/pkt_8x32.c
index 207bc75..56bc3b8 100644
--- a/hdl/fpga/platforms/common/bladerf/software/bladeRF_nios/src/pkt_8x32.c
+++ b/hdl/fpga/platforms/common/bladerf/software/bladeRF_nios/src/pkt_8x32.c
@@ -28,6 +28,7 @@
 #include "pkt_8x32.h"
 #include "devices.h"
 #include "debug.h"
+#include "legion_cmds.h"
 
 static inline bool perform_read(uint8_t id, uint8_t addr, uint32_t *data)
 {
@@ -62,6 +63,17 @@ static inline bool perform_read(uint8_t id, uint8_t addr, uint32_t *data)
             return false;
 #endif  // BOARD_BLADERF_MICRO
 
+#ifdef LEGION_FPGA
+        case NIOS_PKT_8x32_TARGET_USR1: {
+            /* LEGION: читаем STATUS-PIO целиком (addr не используется) */
+            if (!legion_reg_read(addr, data)) {
+                *data = 0x00;
+                return false;
+            }
+            break;
+        }
+#endif
+
         default:
             DBG("Invalid id: 0x%x\n", id);
             *data = 0x00;
@@ -102,6 +114,15 @@ static inline bool perform_write(uint8_t id, uint8_t addr, uint32_t data)
             break;
 #endif  // BOARD_BLADERF_MICRO
 
+#ifdef LEGION_FPGA
+        case NIOS_PKT_8x32_TARGET_USR1:
+            /* LEGION: запись регистра через PIO (см. legion_cmds.c) */
+            if (!legion_reg_write(addr, data)) {
+                return false;
+            }
+            break;
+#endif
+
         default:
             DBG("Invalid id: 0x%x\n", id);
             return false;
