--- /tmp/bladerf-hdl/hdl/fpga/platforms/common/bladerf/software/bladeRF_nios/src/pkt_8x32.c	2026-08-21 16:43:20.676270246 +0000
+++ /tmp/pkt_8x32-legion.c	2026-08-21 17:07:35.169516038 +0000
@@ -28,6 +28,7 @@
 #include "pkt_8x32.h"
 #include "devices.h"
 #include "debug.h"
+#include "legion_cmds.h"
 
 static inline bool perform_read(uint8_t id, uint8_t addr, uint32_t *data)
 {
@@ -62,6 +63,17 @@
             return false;
 #endif  // BOARD_BLADERF_MICRO
 
+#ifdef LEGION_FPGA
+        case NIOS_PKT_8x32_TARGET_USR1: {
+            /* LEGION: читаем STATUS-PIO целиком (addr не используется) */
+            if (!legion_reg_read(addr, data)) {
+                *data = 0x00;
+                return false;
+            }
+            break;
+        }
+#endif
+
         default:
             DBG("Invalid id: 0x%x\n", id);
             *data = 0x00;
@@ -102,6 +114,15 @@
             break;
 #endif  // BOARD_BLADERF_MICRO
 
+#ifdef LEGION_FPGA
+        case NIOS_PKT_8x32_TARGET_USR1:
+            /* LEGION: запись регистра через PIO (см. legion_cmds.c) */
+            if (!legion_reg_write(addr, data)) {
+                return false;
+            }
+            break;
+#endif
+
         default:
             DBG("Invalid id: 0x%x\n", id);
             return false;
