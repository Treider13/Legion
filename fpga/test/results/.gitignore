# Артефакты стендовых прогонов (приёмка E1–E6, soak) не коммитятся
# автоматически — это локальные логи конкретного стенда. Реальный прогон,
# подтверждающий готовность платы, добавляется осознанно: git add -f.
*
!.gitignore
