# LEGION — главное окно

Рабочее окно в приложении (`app/src/App.tsx`). 3D-глаз (`Scene` / `CyborgEye`) не менялся.

| Кадр | Что видно |
|---|---|
| `app-main.png` | глаз + полоса 2400–2500 + Запустить |
| `app-start.png` | коридор F1–F2 и `WAVE_CATALOG` |
| `app-live.png` | скан идёт, кнопка Стоп |
| `app-live-tone.png` | засечка на усилитель, 200 µs host |
| `app-settings.png` | лаборатория; FPGA остаётся здесь |

Старые `v1-sitroom` / `v2-norad` / `v3-houston` — отвергнутый костюм, не продукт.
